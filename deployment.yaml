apiVersion: apps/v1
kind: Deployment
metadata:
  name: dockerfile
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dockerfile
  template:
    metadata:
      labels:
        app: dockerfile
    spec:
      containers:
      - name: python-app
        image: dockerfile
        ports:
        - containerPort: 5000
